package epidemic_core.message.FromUI;

public class EndMessage {
    // Constructor
    public EndMessage(){}

    public static EndMessage decodeMessage(String message) {
        return new EndMessage();
    }
}
